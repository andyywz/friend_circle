<h1>Log In</h1>

<%= form_for(@user, :url => session_index_url) do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Login"%>
<% end %> <br>

<a href="<%= new_user_url %>">Create an account</a>
